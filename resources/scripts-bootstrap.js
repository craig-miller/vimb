/* zentoo bootstrap: enable Dark Reader auto — honors prefers-color-scheme,
 * which vimb broadcasts via `set dark-mode=on/off`. Noctalia flips this
 * on system theme change (SIGUSR2 → config reload). */
DarkReader.auto({ brightness: 100, contrast: 100, sepia: 0 });
